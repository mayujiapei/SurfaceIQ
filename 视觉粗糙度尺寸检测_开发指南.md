# 视觉粗糙度+尺寸检测：软件开发指南（从环境搭建到上线）

> 适用前提：暂不考虑硬件，先在 Kaggle 开源数据集上把算法管线跑通；硬件到位后同一套代码换数据即可。
> 读者：Java 背景、能用 AI 辅助编程的工程师。

---

## 一、技术栈总览

| 层 | 技术 | 用途 | 备注 |
|---|---|---|---|
| 语言 | **Python 3.10+** | 全部算法代码 | 视觉生态唯一选择；别用 Java 写算法 |
| 图像处理 | **OpenCV**（opencv-python） | 读图、灰度、ROI、边缘检测、标定 | 工业视觉标准库 |
| 纹理特征 | **scikit-image** | GLCM 灰度共生矩阵特征 | 粗糙度经典特征 |
| 传统机器学习 | **scikit-learn** + joblib | SVM 分类、数据标准化、模型保存 | 小数据量下最稳 |
| 深度学习 | **PyTorch + torchvision** | ResNet18 迁移学习 | 训练用 Google Colab 免费 GPU |
| 数据处理 | numpy / pandas / matplotlib | 数值计算、报表、可视化 | 标配 |
| 科学计算 | scipy | 偏度/峰度等统计特征 | GLCM 的补充特征 |
| 相机SDK（后期） | 海康 MVS + Python SDK | 工业相机采图 | 硬件到位后再接 |
| 推理服务（后期） | **FastAPI** | 把模型包成 REST 接口 | 你的 Java 业务系统直接 HTTP 调用 |
| 上位机/业务侧（后期） | **你熟悉的 Java**（Spring Boot） | 结果入库、SPC 报表、MES 对接 | 复用停车场项目架构经验 |

**架构图（后期上线形态，现在只需做左半部分）：**

```
[相机] → [Python 采集+推理服务 FastAPI] ←HTTP→ [Java 业务系统: 报表/MES/追溯]
              ↑ 本指南覆盖到这里                    ↑ 后期做，你的主场
```

---

## 二、第 0 步：环境搭建（10 分钟）

```bash
# 1. 安装 Python 3.10+（Windows 官网安装包即可，勾选 Add to PATH）
# 2. 建项目目录和虚拟环境
mkdir vision-roughness && cd vision-roughness
python -m venv venv
venv\Scripts\activate        # Windows
# source venv/bin/activate   # Linux/Mac

# 3. 安装依赖
pip install opencv-python scikit-image scikit-learn scipy numpy pandas matplotlib joblib
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# ↑ 本地先装 CPU 版跑通流程；训练 ResNet 用 Colab 免费 GPU，不用本地装 CUDA

# 4. 验证
python -c "import cv2, skimage, sklearn, torch; print(cv2.__version__, torch.__version__)"
```

配套项目文件已随本指南给出（见附录），目录结构：

```
vision-roughness/
├── requirements.txt
├── data/                    # 数据集放这里
├── models/                  # 训练好的模型存这里（带版本号）
├── cache/                   # 特征缓存（首次提取后自动保存）
├── experiments/             # 实验日志（每次训练自动追加）
└── src/
    ├── features.py          # 特征提取（GLCM+统计特征）
    ├── train_glcm_svm.py    # Baseline A：传统机器学习
    ├── train_resnet.py      # Baseline B：深度学习迁移学习
    ├── infer.py             # 统一推理脚本
    ├── calibrate.py         # 尺寸测量：标定
    └── measure.py           # 尺寸测量：在线测量
```

---

## 三、第 1 步：获取开源数据集（15 分钟）

1. 注册 Kaggle 账号，搜索 **"Surface Roughness Classification"**（sahini 发布）。
2. 网页直接下载 zip（无需 API），解压到 `data/EDM/`。
3. 数据说明：电火花加工表面的显微镜图像，按粗糙度分 **16 个等级**，每个等级约 240 张，含 50X/100X/200X 三种放大倍数。
4. 整理成下面结构（**所有后续代码都假设这个结构**——每个子文件夹名就是类别标签）：

```
data/EDM/50X/
├── class_01/   (240张图)
├── class_02/
└── ...class_16/
```

> **数据映射说明**：原始数据集按拍摄角度命名（00°/03°/06°...45°），共 16 个角度文件夹。我们按顺序映射为 `class_01`~`class_16`，映射关系：
> `00→class_01, 03→class_02, 06→class_03, ..., 45→class_16`
> 50X 的 class_15/class_16 原始数据有 480 张（其余 240 张），不影响训练，数据多反而更好。

> 许可提醒：该数据集仅限科研/学习用途。练手没问题；最终商用模型用你自己拍的数据重训（第 5 步），不冲突。

**第一个练习**：写个脚本统计每个类多少张图、随机显示 9 张——熟悉 OpenCV 读图。10 行代码的事，让 AI 帮你写，但你要看懂每一行。

---

## 四、第 2 步：Baseline A —— GLCM + SVM（第 1 周）

**原理回顾**（张永伟、邓植中论文的路线）：表面越粗糙，图像纹理越"乱"。GLCM（灰度共生矩阵）统计像素对在不同方向/距离上的灰度关系，从中提取对比度、能量、熵等数值特征，再用 SVM 学"特征 → 粗糙度等级"的映射。

### 4.1 特征提取 `src/features.py`

```python
"""从一张灰度图提取纹理+统计特征，输出一维特征向量"""
import cv2
import numpy as np
from skimage.feature import graycomatrix, graycoprops
from scipy import stats

def extract_features(image_path, distances=(1, 2, 4), angles=(0, 45, 90, 135)):
    img = cv2.imread(str(image_path), cv2.IMREAD_GRAYSCALE)
    if img is None:
        raise ValueError(f"读不到图片: {image_path}")

    # --- GLCM 纹理特征 ---
    angles_rad = [np.deg2rad(a) for a in angles]
    glcm = graycomatrix(img, distances=list(distances), angles=angles_rad,
                        levels=256, symmetric=True, normed=True)
    feats = []
    for prop in ['contrast', 'dissimilarity', 'homogeneity', 'energy', 'correlation']:
        vals = graycoprops(glcm, prop)          # shape: (距离数, 方向数)
        feats += [vals.mean(), vals.std()]       # 每个属性2维，5个属性共10维

    # --- 灰度统计特征 ---
    flat = img.ravel().astype(np.float64)
    feats += [flat.mean(), flat.std(),
              float(stats.skew(flat)), float(stats.kurtosis(flat))]  # 4维
    return np.array(feats, dtype=np.float32)     # 总计14维（10+4）
```

### 4.2 训练脚本 `src/train_glcm_svm.py`

```python
"""GLCM + SVM 基线：遍历数据集 → 提特征 → 训练 → 评估 → 保存模型"""
import joblib, numpy as np
from pathlib import Path
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
from sklearn.pipeline import Pipeline
from sklearn.svm import SVC
from sklearn.metrics import classification_report
from features import extract_features

DATA_DIR = Path("data/EDM/50X")   # 先用一种放大倍数
CACHE_DIR = Path("cache")
CACHE_DIR.mkdir(exist_ok=True)

# 1. 遍历数据集，提取特征（首次提取后缓存为 .npy，后续直接加载）
cache_file = CACHE_DIR / "glcm_features_50X.npy"
label_file = CACHE_DIR / "glcm_labels_50X.npy"
if cache_file.exists():
    print("加载缓存特征...")
    X = np.load(cache_file, allow_pickle=False)
    y = np.load(label_file, allow_pickle=False)
else:
    print("首次提取特征（需几分钟）...")
    X, y = [], []
    for class_dir in sorted(DATA_DIR.iterdir()):
        if not class_dir.is_dir():
            continue
        for img_path in class_dir.glob("*.jpg"):   # 数据为 .jpg 格式
            X.append(extract_features(img_path))
            y.append(class_dir.name)
    X, y = np.array(X), np.array(y)
    np.save(cache_file, X)
    np.save(label_file, y)
    print(f"特征已缓存到 {cache_file}")
print(f"共 {len(y)} 张图，特征维度 {X.shape[1]}")

# 2. 划分训练/测试集（分层抽样保证每类比例一致）
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.2, stratify=y, random_state=42)

# 3. 标准化 + SVM，串成 Pipeline（保存后推理时不用重复写预处理）
model = Pipeline([
    ("scaler", StandardScaler()),
    ("svm", SVC(kernel="rbf", C=10, gamma="scale",
                class_weight="balanced", probability=True)),
])
model.fit(X_train, y_train)

# 4. 评估：16 分类准确率目标 ≥80% 即算管线跑通
y_pred = model.predict(X_test)
report = classification_report(y_test, y_pred)
print(report)

# 5. 保存模型（带版本号）+ 记录实验日志
from datetime import datetime
version = datetime.now().strftime("%Y%m%d_%H%M")
Path("models").mkdir(exist_ok=True)
joblib.dump(model, f"models/v1_glcm_svm_{version}.pkl")

Path("experiments").mkdir(exist_ok=True)
with open("experiments/log.txt", "a", encoding="utf-8") as f:
    f.write(f"\n=== {version} GLCM+SVM ===\n")
    f.write(f"数据: {DATA_DIR}\n")
    f.write(f"样本数: {len(y)}, 特征维度: {X.shape[1]}\n")
    f.write(report + "\n")
print(f"模型已保存到 models/v1_glcm_svm_{version}.pkl")
```

### 4.3 这一步你要学会的东西

- **为什么分层抽样**：类别不均衡时随机划分会让某些类全进训练集。
- **为什么用 Pipeline**：标准化参数必须来自训练集，Pipeline 防止"数据泄漏"（这是新手最常犯的错）。
- **调参直觉**：`C` 控制容错，太大过拟合；`gamma` 控制单个样本影响范围。先跑通再调。

---

## 五、第 3 步：Baseline B —— ResNet18 迁移学习（第 2 周）

**原理回顾**（黄杰锋论文路线）：不手工设计特征，让 CNN 自己学。用 ImageNet 上预训练好的 ResNet18（已经会看"纹理、边缘"），只在你自己的数据上微调最后一层/全部层。

### 5.1 训练脚本 `src/train_resnet.py`

```python
"""ResNet18 迁移学习：ImageFolder 加载 → 微调 → 评估 → 保存"""
import json, torch, torch.nn as nn
from pathlib import Path
from torch.utils.data import DataLoader, Subset
from torchvision import datasets, transforms, models

DATA_DIR = "data/EDM/50X"
EPOCHS, BATCH, LR = 15, 32, 1e-4
device = "cuda" if torch.cuda.is_available() else "cpu"
print("使用设备:", device)

# 1. 数据变换：训练集做数据增强（随机翻转/旋转/亮度抖动），测试集只做规范化
train_tf = transforms.Compose([
    transforms.Grayscale(num_output_channels=3),   # 灰度图转3通道适配预训练模型
    transforms.Resize((224, 224)),
    transforms.RandomHorizontalFlip(),
    transforms.RandomRotation(15),
    transforms.ColorJitter(brightness=0.2, contrast=0.2),
    transforms.ToTensor(),
    transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225]),
])
test_tf = transforms.Compose([
    transforms.Grayscale(num_output_channels=3),
    transforms.Resize((224, 224)),
    transforms.ToTensor(),
    transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225]),
])

# 2. 加载数据集并划分（分别创建带不同 transform 的 dataset，避免共享引用）
full = datasets.ImageFolder(DATA_DIR)
n_test = int(len(full) * 0.2)
n_total = len(full)
indices = torch.randperm(n_total, generator=torch.Generator().manual_seed(42)).tolist()
train_indices = indices[:n_total - n_test]
test_indices = indices[n_total - n_test:]

train_full = datasets.ImageFolder(DATA_DIR, transform=train_tf)
test_full = datasets.ImageFolder(DATA_DIR, transform=test_tf)
train_set = Subset(train_full, train_indices)
test_set = Subset(test_full, test_indices)

train_loader = DataLoader(train_set, BATCH, shuffle=True, num_workers=2)
test_loader = DataLoader(test_set, BATCH, num_workers=2)
num_classes = len(full.classes)
print(f"{len(full)} 张图，{num_classes} 类")

# 3. 模型：预训练 ResNet18，替换最后的全连接层
model = models.resnet18(weights=models.ResNet18_Weights.DEFAULT)
model.fc = nn.Linear(model.fc.in_features, num_classes)
model = model.to(device)

# 4. 训练循环
optimizer = torch.optim.Adam(model.parameters(), lr=LR)
criterion = nn.CrossEntropyLoss()
best_acc = 0.0
for epoch in range(EPOCHS):
    model.train()
    for imgs, labels in train_loader:
        imgs, labels = imgs.to(device), labels.to(device)
        loss = criterion(model(imgs), labels)
        optimizer.zero_grad(); loss.backward(); optimizer.step()

    # 每个 epoch 在测试集上评估
    model.eval(); correct = 0
    with torch.no_grad():
        for imgs, labels in test_loader:
            pred = model(imgs.to(device)).argmax(1)
            correct += (pred == labels.to(device)).sum().item()
    acc = correct / n_test
    print(f"epoch {epoch+1}/{EPOCHS}  loss={loss.item():.4f}  test_acc={acc:.4f}")
    if acc > best_acc:                      # 保存最好的模型
        best_acc = acc
        Path("models").mkdir(exist_ok=True)
        torch.save(model.state_dict(), "models/resnet18.pth")
        json.dump(full.class_to_idx, open("models/class_to_idx.json", "w"))

# 5. 记录实验日志
from datetime import datetime
version = datetime.now().strftime("%Y%m%d_%H%M")
Path("experiments").mkdir(exist_ok=True)
with open("experiments/log.txt", "a", encoding="utf-8") as f:
    f.write(f"\n=== {version} ResNet18 ===\n")
    f.write(f"数据: {DATA_DIR}, Epochs: {EPOCHS}, LR: {LR}, Batch: {BATCH}\n")
    f.write(f"最佳测试准确率: {best_acc:.4f}\n")

print(f"最佳测试准确率: {best_acc:.4f}，模型已保存")   # 目标 ≥95%
```

### 5.2 在 Colab 上跑（本地没有 GPU 也没关系）

1. 打开 colab.google，新建笔记本，右上角"修改 → 笔记本设置 → GPU（T4）"。
2. 上传数据集 zip，解压；上传 `src/` 目录。
3. `!pip install scikit-image` 后直接运行脚本——15 轮训练约 10~20 分钟。
4. 训练完把 `models/resnet18.pth` 下载回本地，推理用 CPU 就够。

### 5.3 这一步你要学会的东西

- **迁移学习为什么有效**：ImageNet 预训练模型的浅层已经会检测边缘/纹理，只需少量数据微调高层。
- **数据增强的意义**：翻转/旋转/亮度抖动 = 模拟现场波动，防过拟合。
- **为什么保存 best 而不是 last**：后期 epoch 可能过拟合。
- **数据划分的坑**：`random_split` 后不能直接替换 `.dataset`（训练集和测试集会共享同一个引用），正确做法是分别创建两个 `ImageFolder` 再用 `Subset` 包装。

---

## 六、第 4 步：统一推理脚本（第 2 周末）

两个模型都训好后，封装成同一个调用接口——**换模型不改业务代码**，这是工程习惯：

```python
# src/infer.py —— 用法: python src/infer.py 某张图.jpg --model glcm
import argparse, json, joblib, cv2, torch, torch.nn as nn
import numpy as np
from pathlib import Path
from features import extract_features

def predict_glcm(img_path):
    model = joblib.load("models/glcm_svm.pkl")
    feat = extract_features(img_path).reshape(1, -1)
    cls = model.predict(feat)[0]
    conf = model.predict_proba(feat).max()
    return cls, float(conf)

def predict_resnet(img_path):
    from torchvision import models, transforms
    class_to_idx = json.load(open("models/class_to_idx.json"))
    idx_to_class = {v: k for k, v in class_to_idx.items()}
    model = models.resnet18()
    model.fc = nn.Linear(model.fc.in_features, len(class_to_idx))
    model.load_state_dict(torch.load("models/resnet18.pth", map_location="cpu"))
    model.eval()
    # 推理 transform 与训练时保持一致：先 Grayscale → Resize → ToTensor → Normalize
    tf = transforms.Compose([
        transforms.ToPILImage(),
        transforms.Grayscale(num_output_channels=3),
        transforms.Resize((224, 224)),
        transforms.ToTensor(),
        transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225]),
    ])
    img = cv2.imread(str(img_path), cv2.IMREAD_GRAYSCALE)
    with torch.no_grad():
        prob = torch.softmax(model(tf(img).unsqueeze(0)), dim=1)
    idx = prob.argmax().item()
    return idx_to_class[idx], prob.max().item()

if __name__ == "__main__":
    ap = argparse.ArgumentParser()
    ap.add_argument("image"); ap.add_argument("--model", default="glcm")
    a = ap.parse_args()
    cls, conf = predict_glcm(a.image) if a.model == "glcm" else predict_resnet(a.image)
    print(f"预测等级: {cls}  置信度: {conf:.2%}")
```

---

## 七、第 5 步：换成自己的数据（硬件到位后，衔接点）

跑通前 4 步后，你就拥有了一条**与数据无关的通用管线**。硬件到位后只做三件事：

1. **按同样的目录结构组织自己的数据**：
   ```
   data/my_parts/
   ├── OK/      ← 粗糙度合格件（用标准样块比对或送检确定）
   └── NG/      ← 不合格件
   ```
   或者按 Ra 等级分 4 个文件夹（0.4/0.8/1.6/3.2），样块各拍 40 张。
2. **重训**：两条 baseline 都跑一遍，小数据量下 GLCM+SVM 往往比深度学习更稳——论文结论也是固定工况下经典方法够用。
3. **输出改成 OK/NG**：把多分类结果按分界值映射成二分类（如 Ra≤1.6 → OK）。

> 红线不变：对外只输出 OK/NG + 置信度，不输出 Ra 数值。

---

## 八、第 6 步：尺寸测量模块（纯 OpenCV，不用深度学习）

尺寸测量是经典几何问题，路线：**标定 → 去畸变 → 边缘/轮廓 → 几何拟合 → 像素换算成毫米 → 对比公差**。

### 8.1 标定 `src/calibrate.py`

```python
"""棋盘格标定：求相机内参+畸变系数；再用已知长度物求 mm/像素"""
import cv2, numpy as np, glob, pickle

PATTERN = (9, 6)        # 棋盘格内角点数（列, 行），按你的标定板改
SQUARE_MM = 10.0        # 每格实际边长 mm

# 1. 准备世界坐标点 (0,0,0)~(8,5,0)
objp = np.zeros((PATTERN[0]*PATTERN[1], 3), np.float32)
objp[:, :2] = np.mgrid[0:PATTERN[0], 0:PATTERN[1]].T.reshape(-1, 2) * SQUARE_MM

obj_pts, img_pts = [], []
for f in glob.glob("data/calibration/*.jpg"):   # 拍 10~20 张不同角度的标定板
    img = cv2.imread(f)
    gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
    ok, corners = cv2.findChessboardCorners(gray, PATTERN)
    if ok:
        corners = cv2.cornerSubPix(gray, corners, (11, 11), (-1, -1),
                                   (cv2.TERM_CRITERIA_EPS+cv2.TERM_CRITERIA_MAX_ITER, 30, 0.001))
        obj_pts.append(objp); img_pts.append(corners)

ret, K, dist, _, _ = cv2.calibrateCamera(obj_pts, img_pts, gray.shape[::-1], None, None)
print(f"标定重投影误差: {ret:.4f} 像素")     # <0.5 像素为合格
pickle.dump({"K": K, "dist": dist}, open("models/camera_calib.pkl", "wb"))
```

毫米换算（固定工作距离下最实用的方法）：在视场里放一个**已知长度的量块/标定尺**，量出图中像素长度，`mm_per_pixel = 实际长度mm / 像素长度`，存进配置文件。

### 8.2 在线测量 `src/measure.py`（骨架，几何项按你的零件改）

```python
"""尺寸测量主流程骨架：去畸变 → 边缘 → 拟合 → 换算 → 判公差"""
import cv2, numpy as np, pickle

calib = pickle.load(open("models/camera_calib.pkl", "rb"))
MM_PER_PIXEL = 0.02      # 由标定尺实测得到，填你的值

def measure_part(img_path, spec_mm, tol_mm):
    img = cv2.imread(str(img_path))
    if img is None:
        return {"result": "NG", "reason": "无法读取图片"}

    img = cv2.undistort(img, calib["K"], calib["dist"])     # 1. 去畸变
    gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
    gray = cv2.GaussianBlur(gray, (5, 5), 0)

    # 2. 边缘检测（现场光照固定后用固定阈值；不稳定用自适应）
    edges = cv2.Canny(gray, 50, 150)

    # 3. 找轮廓，取最大轮廓做最小外接矩形（测长宽场景）
    contours, _ = cv2.findContours(edges, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_NONE)
    if not contours:
        return {"result": "NG", "reason": "未检测到轮廓"}

    rect = cv2.minAreaRect(max(contours, key=cv2.contourArea))
    w_px, h_px = rect[1]

    # 4. 像素 → 毫米，对比公差
    w_mm, h_mm = w_px * MM_PER_PIXEL, h_px * MM_PER_PIXEL
    ok = abs(w_mm - spec_mm) <= tol_mm
    return {"width_mm": round(w_mm, 3), "height_mm": round(h_mm, 3),
            "result": "OK" if ok else "NG"}
```

更高精度时用**亚像素边缘**（`cv2.cornerSubPix` 思路对边缘点亚像素化，或直接用 `findContours` 后对轮廓点拟合直线/圆），500 万像素相机重复精度可达 ±0.02~0.05mm。

> 捷径提醒：海康免费的 **VM 算法平台**内置标定、边缘查找、几何测量工具，可视化拖拽配置，很多场景零代码——到时候先试试 VM 能不能直接满足，满足就不用写 8.2。

---

## 九、第 7 步：工程化上线（后期，简要）

1. **采集替换**：海康 MVS 装好 → Python SDK（MvImport）替换 `cv2.imread`，触发模式采图。
2. **服务化**：FastAPI 把 `infer.py`/`measure.py` 包成 POST 接口（传图返回 JSON），代码 30 行以内。
3. **业务侧**：你的 Java 系统调 HTTP 接口拿结果，入库、SPC 报表、追溯——和你停车场项目"设备侧 adapter → 业务侧"的分层一模一样。
4. **PLC 对接**：OK/NG 通过 pymodbus 或串口IO卡发给 PLC，触发剔除。

---

## 十、执行顺序总表

| 顺序 | 任务 | 用时参考 | 验收标准 |
|---|---|---|---|
| 0 | 环境搭建 | 半天 | import 全部通过 |
| 1 | 下载整理 Kaggle 数据集 | 半天 | 目录结构正确，能显示图片 |
| 2 | GLCM+SVM 跑通 | 3~5 天 | 16 分类准确率 ≥80% |
| 3 | ResNet18 跑通（Colab） | 3~5 天 | 16 分类准确率 ≥95% |
| 4 | 统一推理脚本 | 半天 | 一张图一行命令出结果 |
| 5 | （硬件后）自建数据重训 | 1~2 周 | 样块分类 ≥95%，零件判定与送检一致 |
| 6 | 尺寸测量模块 | 1 周 | 标定误差 <0.5px，重复精度 ±0.05mm |
| 7 | 工程化上线 | 2~3 周 | 全链路自动化跑通 |

> **时间说明**：步骤 2/3 给 Java 背景工程师留了更多缓冲，首次接触 Python 视觉生态需要理解 GLCM 原理、调试特征提取、调 SVM 参数等，不宜赶进度。

---

## 十一、工程化补充建议

以下是原指南未涉及但在实际开发中很重要的几点：

### 11.1 特征缓存

GLCM 特征提取非常耗时（每张图都要计算灰度共生矩阵）。已在 `train_glcm_svm.py` 中加入缓存机制：首次提取后保存为 `.npy` 文件到 `cache/`，后续训练直接加载，调参不再重复等待。

### 11.2 实验日志

每次训练自动追加到 `experiments/log.txt`，记录时间戳、参数、准确率。方便对比不同参数下的模型表现，避免“调完忘了哪个最好”。

### 11.3 模型版本管理

模型文件带版本号：`models/v1_glcm_svm_20260718_1430.pkl`。避免新模型覆盖旧模型后发现回不去。

### 11.4 交叉验证（可选）

16 分类任务用单次 80/20 划分可能因数据分布不均导致结果波动。如果时间允许，建议用 5-fold 交叉验证：
```python
from sklearn.model_selection import cross_val_score
scores = cross_val_score(model, X, y, cv=5)
print(f"CV 准确率: {scores.mean():.4f} ± {scores.std():.4f}")
```

### 11.5 异常保护

`measure.py` 已加入空轮廓保护和图片读取失败处理。生产环境中任何外部输入都可能异常，必须防御性编程。

---

**学习建议**：每一步先让 AI 生成代码，然后逐行看懂再运行；跑通后故意改坏一个参数看会出什么错——这是你建立“读代码”习惯的好机会，这套项目每一步都对应论文里的真实方法，比看教程扎实得多。
