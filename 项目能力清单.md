# RoughSense（锐感视觉）项目能力清单

> 本文档汇总 `vision-roughness/` 项目当前已具备的能力、技术规格、使用方式及边界条件，供内部交接、方案汇报和后续扩展参考。
> 最后更新：2026-07-30

---

## 一、项目定位

**基于机器视觉的 EDM 工件表面粗糙度分类 + 尺寸检测系统。**

- 当前阶段：算法管线已跑通，基于 Kaggle 开源数据集训练并选型完毕，等待工业相机、标准样块和标定板到位后进入工程化部署。
- 目标场景：工业质检产线，相机采图 → Python 推理 → 输出粗糙度等级/OK/NG 及尺寸结果。

---

## 二、已具备的核心能力

### 2.1 表面粗糙度分类

| 能力项 | 说明 |
|---|---|
| 任务类型 | 16 分类（对应 EDM 加工角度 0°~45°，每 3° 一类） |
| 主模型 | ResNet18 迁移学习（ImageNet 预训练，微调全连接层） |
| 备用模型 | GLCM + SVM（传统纹理特征 + 支持向量机） |
| 当前最佳准确率 | **ResNet18：95.60%**（v2 复训，独立验证集选模，测试集仅最终评估一次） |
| 备用模型准确率 | GLCM+SVM：89.93% |
| 推理速度 | ResNet18：约 26 ms/张（GPU）；GLCM+SVM：约 167 ms/张（CPU） |
| 模型大小 | ResNet18：42.7 MB；GLCM+SVM：586 KB |
| 输出 | 预测类别（class_01 ~ class_16）+ 置信度 |

### 2.2 统一推理入口

- 文件：`vision-roughness/src/infer.py`
- 支持模式：
  - 单张图片推理
  - 文件夹批量推理
  - 输出 CSV 报表
- 模型切换：`--model resnet` 或 `--model glcm`
- 示例：
  ```powershell
  python src/infer.py data/EDM/50X/class_01/1.jpg --model resnet
  python src/infer.py data/EDM/50X/ --model resnet --batch --output results.csv
  ```

### 2.3 模型对比与选型

- 文件：`vision-roughness/src/compare_models.py`
- 能力：在同一份测试集上自动对比 GLCM+SVM 与 ResNet18 的准确率、推理速度、模型大小，并输出选型建议。
- 当前结论：**生产环境推荐 ResNet18**，GLCM+SVM 作为离线备份和可解释性对照。

### 2.4 数据探索与可视化

- 文件：`vision-roughness/src/explore.py`
- 能力：
  - 统计每个类别的图片数量
  - 计算各类别灰度均值/标准差
  - 随机抽样生成可视化网格图（保存为 `data/sample_grid.png`）

### 2.5 尺寸测量（骨架已具备）

- 文件：`vision-roughness/src/calibrate.py` + `vision-roughness/src/measure.py`
- 能力：
  - 棋盘格相机标定，输出内参矩阵 `K` 和畸变系数 `dist`
  - 在线测量流程：去畸变 → 高斯模糊 → Canny 边缘 → 找轮廓 → 最小外接矩形 → 像素转毫米 → 公差判定
- 当前状态：代码骨架完成，但缺少实际标定板照片和 `MM_PER_PIXEL` 实测值，无法直接运行。

---

## 三、数据能力

| 能力项 | 说明 |
|---|---|
| 当前数据集 | Kaggle "Surface Roughness Classification"（EDM 表面图像） |
| 数据组织 | 按放大倍数和类别分文件夹：`data/EDM/50X/class_01/` ~ `class_16/` |
| 放大倍数 | 50X（主训练集）、100X、200X（已整理但未用于主模型） |
| 样本规模 | 50X 共 4320 张；100X/200X 各 3840 张 |
| 类别映射 | 原始角度文件夹 00/03/06/.../45 映射为 class_01 ~ class_16 |
| 特征缓存 | GLCM 特征首次提取后缓存为 `cache/glcm_features_50X.npy`，避免重复计算 |
| 数据增强 | ResNet18 训练时使用随机翻转、旋转、亮度/对比度抖动 |

---

## 四、技术能力

### 4.1 技术栈

| 层级 | 技术 |
|---|---|
| 开发语言 | Python 3.11 |
| 图像处理 | OpenCV 5.0 |
| 深度学习框架 | PyTorch 2.8 + torchvision（CUDA 12.9） |
| 传统机器学习 | scikit-learn 1.9 + joblib |
| 纹理特征 | scikit-image（GLCM 灰度共生矩阵） |
| 科学计算 | numpy、pandas、scipy、matplotlib |

### 4.2 训练能力

- **ResNet18 训练**：`src/train_resnet.py`
  - 自动划分训练/验证/测试集（72%/8%/20%）
  - 按验证集准确率保存最优模型
  - 支持本地 GPU 训练（RTX 5060 Laptop，约 3 分钟跑完 30 epochs）
  - 也提供 Colab Notebook：`colab_train_resnet.ipynb`
- **GLCM+SVM 训练**：`src/train_glcm_svm.py`
  - 自动遍历数据集提取 14 维特征
  - 特征缓存机制
  - 分层抽样划分训练/测试集
  - Pipeline（StandardScaler + SVC）防止数据泄漏
  - 自动保存带时间戳版本化模型和实验日志

### 4.3 实验管理能力

- 训练结果自动追加到 `experiments/log.txt`
- 模型文件带版本号，如 `v1_glcm_svm_20260718_1245.pkl`
- 支持模型对比报告自动写入日志

---

## 五、使用方式

### 5.1 环境激活

```powershell
cd E:\lingjianjiagongjiance\vision-roughness
.\venv\Scripts\activate
```

### 5.2 常用命令

| 操作 | 命令 |
|---|---|
| 单张推理 | `python src\infer.py data\EDM\50X\class_01\1.jpg --model resnet` |
| 批量推理 | `python src\infer.py data\EDM\50X\ --model resnet --batch --output results.csv` |
| 模型对比 | `python src\compare_models.py` |
| 数据探索 | `python src\explore.py` |
| 训练 GLCM+SVM | `python src\train_glcm_svm.py` |
| 训练 ResNet18 | `python src\train_resnet.py` |

---

## 六、集成与扩展能力

### 6.1 未来可扩展的接口

| 扩展方向 | 说明 |
|---|---|
| FastAPI 服务 | 将 `infer.py`/`measure.py` 包装为 REST API，供 Java 业务系统 HTTP 调用 |
| Java 业务侧 | 结果入库、SPC 报表、MES 追溯 |
| 工业相机对接 | 海康 MVS Python SDK 替换 `cv2.imread`，实现触发采图 |
| PLC 信号输出 | 通过 pymodbus 或串口 IO 卡发送 OK/NG 信号触发剔除 |
| 自建数据重训 | 按 `data/my_parts/OK/`、`data/my_parts/NG/` 组织实际零件数据后重新训练 |

### 6.2 输出契约（当前）

- 粗糙度分类：`{"prediction": "class_05", "confidence": 0.9823}`
- 尺寸测量：`{"width_mm": 12.345, "height_mm": 8.765, "result": "OK"}` 或 `{"result": "NG", "reason": "未检测到轮廓"}`

---

## 七、边界条件与限制

1. **数据域限制**：当前 95.60% 准确率基于 Kaggle 开源数据；实际产线的光照、对焦、污染会导致精度下降，需用自采数据重训。
2. **尺寸测量未标定**：`calibrate.py` 需要实际标定板照片，`measure.py` 的 `MM_PER_PIXEL` 需要实测填入。
3. **模型版本**：GLCM+SVM 模型由 sklearn 1.3.2 训练，当前环境 1.9.0 功能正常但会有版本警告。
4. **Windows 多进程**：DataLoader 已设置 `num_workers=0`，避免 Windows spawn 模式问题。
5. **硬件依赖**：ResNet18 推理可用 CPU，但训练和生产环境推荐 GPU；尺寸测量模块需工业相机和标定板。

---

## 八、关键文件索引

| 文件 | 作用 |
|---|---|
| `视觉粗糙度尺寸检测_开发指南.md` | 完整开发教程，从环境搭建到上线 |
| `vision-roughness/PROJECT_STATUS.md` | 项目状态报告、训练结果、里程碑 |
| `vision-roughness/requirements.txt` | Python 依赖列表 |
| `vision-roughness/src/features.py` | GLCM + 统计特征提取 |
| `vision-roughness/src/train_glcm_svm.py` | GLCM+SVM 训练 |
| `vision-roughness/src/train_resnet.py` | ResNet18 训练 |
| `vision-roughness/src/infer.py` | 统一推理入口 |
| `vision-roughness/src/compare_models.py` | 模型对比 |
| `vision-roughness/src/explore.py` | 数据探索 |
| `vision-roughness/src/calibrate.py` | 相机标定 |
| `vision-roughness/src/measure.py` | 尺寸测量 |
| `vision-roughness/models/resnet18.pth` | 当前最佳 ResNet18 模型 |
| `vision-roughness/models/v1_glcm_svm_*.pkl` | GLCM+SVM 模型 |
| `vision-roughness/experiments/log.txt` | 训练与对比日志 |
